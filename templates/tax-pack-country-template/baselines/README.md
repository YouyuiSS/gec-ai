# Baselines

After the first trusted parse, create:

```text
baselines/
  xx-example-profile/
    field_catalog.json
    baseline_meta.json
```

Do not promote a baseline until the output has been manually checked and the quality gate passes.
